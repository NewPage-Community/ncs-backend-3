syntax = "proto3";

package ncs.hello;
option go_package = "grpc";

service Hello {
    rpc say (SayReq) returns (SayResp);
}

message SayReq {
    string Message = 1;
}

message SayResp {
    string Message = 1;
}